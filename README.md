# Angular 2 練習：n2-github-profile

Angular 2 練習，以 [Angular QuickStart](https://github.com/angular/quickstart) 為底，使用版本為 rc4。

輸入 GitHub 帳號，透過 GitHub api 取得資料，顯示該帳號資料，並以分頁方式顯示 repo 列表。

## 使用

```bash
npm install
npm start
```
